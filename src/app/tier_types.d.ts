type Tier = {
    /**
    * User id as defined by the database
    */
    id: number,
    /**
    *Users username as entered when created
    */
    petition_id: number,
    title: string,
    description: string,
    cost: number

}